{
  "name": "somtum1pos-v3",
  "version": "3.0.0",
  "description": "POS QR Order ร้านส้มตำนายหนึ่ง — Node.js + SQLite",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "test": "node scripts/ci-test.js",
    "syntax": "node --check server/index.js && node --check server/db.js && node --check server/password.js && node --check server/slip.js && node --check server/auth.js"
  },
  "engines": {
    "node": ">=22"
  },
  "private": true
}
